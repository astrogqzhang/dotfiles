- default:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.jupyter: jupyter
    ~/.condarc: condarc
    ~/.spacemacs.d: spacemacs.d
    ~/.tmux.conf.local : tmux.conf


- shell:
    - git submodule update --remote
    - sh $HOME/.dotfiles/bin/update-other
    

